import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  main: {
    marginTop: "20px",
  },
  totalMonthlySpent: {
    color: "blue",
  },
  noHistoryData: {
    color: "red",
    fontSize: "x-large",
  },
}));
