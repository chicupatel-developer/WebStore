import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  main: {
    margin: "100px",
  },
  controlInvalid: {
    color: "red",
  },
  errorList: {
    marginTop: "10px",
    marginBottom: "10px",
  },
}));
